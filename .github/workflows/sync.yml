#name: Build & Deploy Safari App Backend to Production Server
#
#on:
#  push:
#    branches:
#      - main
#
#jobs:
#  build:
#    name: Build & Push Docker Image
#    runs-on: ubuntu-latest
#    environment: production
#
#    steps:
#      - name: Checkout source
#        uses: actions/checkout@v4
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Login to Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKER_USERNAME }}
#          password: ${{ secrets.DOCKER_PASSWORD }}
#
#      - name: Build Docker image
#        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/rnsafariback:latest .
#
#      - name: Push Docker image
#        run: docker push ${{ secrets.DOCKER_USERNAME }}/rnsafariback:latest
#
#  deploy:
#    name: Deploy to Production Server
#    runs-on: ubuntu-latest
#    needs: build
#    environment: production
#
#    steps:
#      - name: Deploy via SSH
#        uses: appleboy/ssh-action@v0.1.10
#        with:
#          host: ${{ secrets.VPS_HOST }}
#          username: ${{ secrets.VPS_USER }}
#          password: ${{ secrets.VPS_PASSWORD }}
#          port: 22
#          script: |
#            echo "Deploying backend to production server..."
#            docker pull ${{ secrets.DOCKER_USERNAME }}/rnsafariback:latest
#            docker stop rnsafariback || true
#            docker rm rnsafariback || true
#            docker run -d --name rnsafariback \
#              -v safari:/mnt \
#              --env-file /root/files/.env \
#              -p 8000:8100 \
#              ${{ secrets.DOCKER_USERNAME }}/rnsafariback:latest
#            docker network connect safaridesk-net rnsafariback
